#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @b: An integer
 *
 * Return: Always 0
 */
int _abs(int b)
{
	int b;

	if (((b - 0) == -b) || ((b + 0) == b))
		_putchar(b);
	return (0);
}
